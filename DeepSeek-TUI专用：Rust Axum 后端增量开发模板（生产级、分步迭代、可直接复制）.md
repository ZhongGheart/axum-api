# DeepSeek\-TUI专用：Rust Axum 后端增量开发模板（生产级、分步迭代、可直接复制）

# 前言（必读）

本模板为**专门适配 DeepSeek\-TUI Agent 模式**设计，采用**增量开发**，拒绝一次性写爆。每一段文案为一个开发阶段，阶段之间不冲突、不覆盖、循序渐进。

技术栈固定：**Axum \+ Tokio \+ SQLx \+ Postgres \+ JWT \+ Tower\-http \+ anyhow\+thiserror**

使用规则：

1. 全程保持 **Agent 模式**（不要YOLO）

2. 每完成一阶段，输入 `:save` 保存会话

3. 每一阶段文案**直接完整粘贴发送**

4. AI生成架构后回复：**确认，开始实施**

---

# 第一阶段：初始化工程 \&amp; 基础骨架（打底）

**使用时机：项目新建第一次发送**

```plain
现在开始第一阶段开发，请严格按照生产级Rust规范初始化项目。
技术栈：Axum 最新稳定版、Tokio、SQLx、PostgreSQL。
本阶段只做基础架构，不写业务代码。
要求：
1. 初始化cargo项目，配置最优Cargo.toml依赖版本
2. 创建标准分层目录结构：common、config、entity、dao、service、handler、middleware
3. 封装全局统一返回结构体、全局错误枚举
4. 配置.env环境文件、数据库连接池、日志组件tracing
5. 编写基础main函数，搭建空路由、健康检查接口 /ping
6. 添加基础.gitignore、dockerignore
7. 代码全部中文注释，严格遵循rustfmt、rust-clippy规范
本阶段禁止编写任何业务登录、业务表、业务逻辑。
请先输出目录架构，我确认后开始创建文件。
```

---

# 第二阶段：中间件 \&amp; 全局基础能力（通用能力）

**使用时机：第一阶段运行正常、能ping通后**

```plain
进入第二阶段增量开发，基于现有代码扩展通用基础能力，不改动原有正常代码。
本阶段开发内容：
1. 基于tower-http实现：CORS跨域、请求日志、限流中间件
2. 实现全局统一异常捕获、panic捕获
3. 封装时间工具、字符串工具、通用常量工具类
4. 完善日志分级：控制台彩色输出、文件滚动保存
5. 优化数据库连接池参数，适配生产环境
6. 统一http状态码、业务错误码枚举
要求：
1. 不破坏原有代码结构
2. 新增代码全部放入common或middleware目录
3. 保持代码风格统一
完成后给出测试方式。
```

---

# 第三阶段：认证体系（JWT \+ 密码加密）

**使用时机：中间件全部完成后**

```plain
进入第三阶段增量开发，实现用户认证底层能力。
开发内容：
1. 基于jsonwebtoken实现JWT签发、解析、过期校验
2. 使用argon2实现密码哈希加密，禁止明文存储
3. 编写JWT鉴权中间件，支持公开接口白名单
4. 编写用户基础数据表user，使用sqlx编写建表SQL
5. 封装token工具、加密工具
要求：
1. 使用纯SQLx，不使用任何ORM
2. 所有密钥、过期时间写入.env可配置
3. 中间件可灵活配置是否需要登录
4. 禁止硬编码敏感信息
```

---

# 第四阶段：核心业务接口（登录、注册、个人信息）

**使用时机：JWT无误、数据库连通正常**

```plain
进入第四阶段增量开发，开发用户基础业务接口。
基于已有架构新增：
1. 用户注册接口：账号密码注册、参数校验、重复判断
2. 用户登录接口：校验密码、签发JWT、返回用户信息
3. 获取当前登录用户信息接口
4. 简单参数校验中间件、请求体校验
5. 完善user表基础字段：账号、密码、昵称、状态、创建时间
要求：
1. 严格分层：handler接收参数、service处理业务、dao操作数据库
2. 所有入参做合法性校验
3. 返回统一格式JSON
4. 新增接口全部挂载路由，区分公开接口、鉴权接口
```

---

# 第五阶段：权限体系（角色\+简单权限）

**使用时机：登录流程完全跑通后**

```plain
进入第五阶段增量开发，增加角色权限控制能力。
开发内容：
1. 新增角色表、用户角色关联表
2. 改写JWT载荷，存入用户ID、角色标识
3. 新增权限校验中间件，支持角色拦截
4. 初始化管理员默认账号、默认角色
5. 提供简单权限测试接口：需要管理员权限访问
要求：
1. 使用事务保证关联数据一致性
2. 提供初始化SQL脚本
3. 权限中间件可灵活注解路由
4. 不改动之前成熟的登录逻辑
```

---

# 第六阶段：缓存 \&amp; 生产优化（Redis、限流、优雅关闭）

**使用时机：基础业务全部稳定**

```plain
进入第六阶段生产优化增量开发。
开发内容：
1. 接入Redis异步客户端，封装通用Redis工具类
2. 实现登录token黑名单、下线失效功能
3. 实现接口限流：单IP限流、单用户限流
4. 增加服务优雅关闭、信号监听
5. 优化数据库SQL、增加索引、优化连接池
6. 增加全局请求ID，链路追踪日志
要求：
1. Redis配置写入.env
2. 限流规则可配置
3. 所有新增代码低侵入、不破坏原有业务
```

---

# 第七阶段：文档、部署、打包、交付

**使用时机：代码全部开发完成**

```plain
进入第七阶段交付优化，为本项目生成生产级交付文件。
请一次性生成：
1. 多阶段优化Dockerfile，最小镜像体积，使用musl静态编译
2. 编写完整README：本地运行、编译、部署、接口说明
3. 整理全部数据库初始化SQL，单独存放sql目录
4. 生成docker-compose.yml，一键启动postgres+redis+服务
5. 整理.env生产模板、开发模板
6. 给出生产部署建议、服务器配置、安全建议
要求：所有文件通俗易懂、注释齐全、可直接上线部署。
```

---

# 附录：通用补充指令（任意阶段可发）

## 补充1：代码检查优化

```plain
请对当前项目进行代码审查，找出不合理写法、潜在bug、性能问题，逐一修改并给出优化说明。严格遵守rust最佳实践。
```

## 补充2：新增业务表通用话术

```plain
现在增量新增一张业务表：xxx，请按照项目现有规范：建表SQL、entity实体、dao层、service层、handler接口，完整新增，不要改动原有成熟代码。
```

## 补充3：修复报错

```plain
当前代码编译报错，请精准定位问题、最小改动修复、解释错误原因。
```

---

# 固定规则（你必须遵守）

1. **绝不一次性写完**：一阶段只干一件事

2. **绝不删改稳定代码**：增量叠加，低侵入

3. **全部中文注释**，适合新手阅读

4. **严格分层**：handler→service→dao

5. **纯SQLx无ORM**，高性能、可控

> （注：文档部分内容可能由 AI 生成）
