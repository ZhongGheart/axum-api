# 适配Rust后端的前端架构设计\+DeepSeek\-TUI增量开发需求模板

# 一、前置说明

本文档**完全配套上文Rust\(Axum\+SQLx\)后端增量项目**，专为DeepSeek\-TUI Agent模式设计。适配后端：JWT鉴权、统一返回格式、RBAC权限、PostgreSQL、Redis限流。前端主打**轻量化、企业级后台、和后端严格对齐、无过度封装**，适合AI一键生成、增量迭代、极少报错。

适用场景：管理后台、中台系统、内部OA、用户权限后台。

# 二、前端技术栈选型（固定、不折腾）

## 2\.1 核心技术栈（生产级主流）

- **构建框架**：Vue3（组合式API）\+ Vite（极速构建）

- **编程语言**：TypeScript（类型严格，匹配Rust强类型思想）

- **UI组件库**：Naive UI（干净无副作用、权限后台最优）

- **路由管理**：Vue Router 4

- **状态管理**：Pinia（替代Vuex，轻量化）

- **网络请求**：Axios（封装拦截器，适配后端JWT）

- **样式方案**：SCSS \+ 原子化CSS

- **图标库**：Iconify

## 2\.2 配套工具库（必备）

- 请求加密：js\-sha256（前端密码加密传输）

- 本地存储：localforage（持久化Token）

- 表单校验：VeeValidate（严格参数校验，对齐后端校验）

- 打包优化：vite\-plugin\-compression（gzip压缩）

# 三、前端架构设计（适配Rust后端特性）

## 3\.1 整体架构原则

1. **强类型对齐**：前端TS类型严格匹配后端Rust结构体，杜绝字段错乱。

2. **拦截器统一处理**：适配后端统一返回格式、错误码、JWT过期。

3. **权限严格受控**：和后端RBAC角色权限联动，路由\+按钮双重权限控制。

4. **轻量化无冗余**：不引入重型依赖，适配后端轻量高性能特性。

5. **目录分层清晰**：完全模块化，适配DeepSeek\-TUI增量开发。

## 3\.2 标准目录结构（固定模板）

```plain
src
├── api                # 接口请求层（按业务模块拆分，和后端controller一一对应）
│   ├── user.ts        # 用户接口（登录、注册、个人信息）
│   ├── role.ts        # 角色权限接口
│   └── common.ts      # 公共接口
├── assets             # 静态资源、全局样式
├── components         # 公共全局组件
│   ├── PageLayout     # 后台布局框架
│   ├── AuthButton     # 权限按钮组件
│   └── ErrorPage      # 异常页面
├── config             # 全局配置
│   ├── env.ts         # 环境变量
│   └── constant.ts    # 全局常量
├── hooks              # 通用组合式钩子
│   ├── useAuth.ts     # 权限钩子
│   └── useRequest.ts  # 请求封装钩子
├── router             # 路由管理
│   ├── modules        # 业务路由拆分
│   └── index.ts       # 路由入口、权限守卫
├── stores             # Pinia状态管理
│   ├── user.ts        # 用户信息、Token、角色
│   └── app.ts         # 全局应用状态
├── types              # TS类型定义（严格复刻后端Rust结构体）
│   ├── user.d.ts
│   └── common.d.ts
├── utils              # 工具类
│   ├── request.ts     # Axios拦截器封装
│   ├── crypto.ts      # 密码加密
│   └── storage.ts     # 本地存储
└── views              # 页面视图
    ├── login          # 登录页
    ├── home           # 首页
    ├── system         # 系统管理（用户、角色）
    └── error          # 错误页面
    
```

## 3\.3 核心适配后端设计（关键）

### 3\.3\.1 适配后端统一返回格式

后端Rust固定返回结构体，前端全局解析：

```typescript
// 对齐后端Rust CommonResult<T>
export interface CommonResult<T> {
  code: number;      // 业务码
  msg: string;        // 提示信息
  data: T;            // 泛型数据
  timestamp: number; // 时间戳
}

```

### 3\.3\.2 JWT鉴权适配

- 请求拦截器：自动携带 Header `Authorization: Bearer Token`

- 响应拦截器：捕获401、403、500错误，自动跳转登录、清除过期Token

- 本地存储：Token加密存储，适配后端Redis黑名单下线逻辑

### 3\.3\.3 权限联动

- 后端返回当前用户角色标识，前端动态生成可访问路由

- 封装权限按钮，无权限自动隐藏，对齐后端接口鉴权

# 四、DeepSeek\-TUI 前端增量开发需求模板（可直接复制）

**使用规则：和后端一致，Agent模式、分段增量、不一次性写完、每阶段保存会话**。

## 阶段一：前端项目初始化（基础骨架）

```plain
现在开始第一阶段前端增量开发，适配已完成的Rust Axum后端。
技术栈：Vue3 + TypeScript + Vite + Naive UI + Pinia + Vue Router。
要求：
1. 初始化规范前端工程，配置vite最优打包参数
2. 搭建标准目录结构，严格对齐后端业务模块
3. 封装Axios全局请求拦截器、响应拦截器，适配后端统一返回格式
4. 配置环境变量，区分开发/生产接口地址
5. 编写全局TS通用类型，复刻后端Rust通用返回结构体
6. 实现基础路由、空白布局、404错误页面
7. 配置ESLint+Prettier，代码格式规范化
8. 生成.gitignore、README、部署说明
本阶段禁止编写业务登录逻辑，只搭建基础架构。
请先输出目录架构，我确认后开始创建文件。
```

## 阶段二：通用工具与状态封装

```plain
进入第二阶段增量开发，基于现有骨架完善通用基础能力。
开发内容：
1. 封装本地存储工具类，实现加密持久化存储
2. 封装密码加密工具，前端使用sha256加密传输密码，不明文提交
3. 配置Pinia全局状态，编写用户状态、全局应用状态
4. 完善请求超时、重试、错误提示逻辑
5. 封装全局消息提示、弹窗工具
6. 编写路由守卫，基础拦截未登录请求
要求：不改动原有架构，新增代码低侵入，严格遵循现有目录规范。
```

## 阶段三：登录认证模块（对接后端JWT）

```plain
进入第三阶段增量开发，对接Rust后端登录认证接口。
开发内容：
1. 编写登录页面、注册页面，简约企业风格
2. 对接后端登录、注册、获取用户信息接口
3. 实现Token存取、过期校验、自动刷新
4. 路由守卫完善：未登录拦截、登录后禁止跳转登录页
5. 适配后端401、403错误码，自动清除登录状态
6. 实现记住密码、退出登录功能，同步后端Redis黑名单下线
要求：
1. TS类型严格匹配后端返回实体
2. 表单增加前端校验，对齐后端参数校验规则
3. 代码注释完善，便于后期维护
```

## 阶段四：权限体系开发（对接后端RBAC）

```plain
进入第四阶段增量开发，联动后端实现角色权限控制。
开发内容：
1. 解析后端返回的用户角色标识
2. 实现动态路由，根据角色自动加载可访问页面
3. 封装权限校验指令、权限按钮组件
4. 编写系统管理页面：用户管理、角色管理
5. 对接后端用户新增、修改、删除、权限分配接口
要求：
1. 实现按钮级、页面级双重权限控制
2. 无权限元素自动隐藏，禁止粗暴禁用
3. 完全复用现有工具类，不重复封装
```

## 阶段五：页面优化与通用业务组件

```plain
进入第五阶段增量开发，优化前端使用体验。
开发内容：
1. 封装通用分页组件、搜索组件、表格组件
2. 增加页面加载动画、请求loading加载状态
3. 完善全局样式、适配暗黑/明亮主题
4. 优化接口请求防抖、节流处理
5. 适配后端限流报错，给出友好提示
要求：组件通用可复用，适配后台绝大多数业务场景。
```

## 阶段六：打包部署与工程优化

```plain
进入第六阶段交付优化，生成生产级部署文件。
开发内容：
1. 配置gzip压缩、资源拆分，优化打包体积
2. 编写Nginx部署配置文件，适配后端接口跨域转发
3. 完善项目README：环境配置、打包命令、部署流程
4. 清除冗余代码、优化依赖、修复潜在bug
5. 适配后端Docker部署，编写前端dockerfile
要求：打包后体积精简，可直接配合Rust后端服务器部署。
```

# 五、前后端联动开发规范（DeepSeek\-TUI专用）

## 5\.1 字段对齐规范

前端TS类型、后端Rust结构体**字段名、字段类型、注释必须完全一致**，禁止随意修改字段，减少联调报错。

## 5\.2 接口命名规范

后端接口路径统一：`/api/v1/模块/接口`，前端api文件按模块拆分，一一对应后端handler。

## 5\.3 联调测试指令（可直接发给AI）

```plain
请检查当前前后端接口连通性，遍历所有已开发接口，模拟正常请求、异常请求、权限不足请求，排查报错、参数不匹配、类型不一致问题，最小改动修复并给出联调报告。
```

# 六、禁忌规则（AI开发强制遵守）

1. 禁止随意新增重型UI依赖、禁止修改既定技术栈；

2. 禁止一次性写完所有页面，严格按阶段增量迭代；

3. 禁止前后端字段不一致，所有实体必须双向对齐；

4. 禁止明文传输敏感数据，密码必须前端加密；

5. 禁止冗余重复封装，复用已有工具类。

> （注：文档部分内容可能由 AI 生成）
